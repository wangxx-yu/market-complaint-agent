## 市场监管 Multi-Agent 投诉智能处理系统

`Python` `FastAPI` `LangGraph` `RAG` `Chroma` `sklearn` `Ollama` `PyYAML`

- **规则引擎配置化**：4 个 YAML 规则文件（accept/reject/dispatch/sensitive，27 条规则）+ RuleLoader + RuleEngine 替代 15+ 个硬编码关键词列表。规则变更无需修改 Python 代码，回放评估可验证准确率不下降。
- **RAG 可信度保障**：50 条 RAG 评估集 + 评估器（Recall@K/MRR）+ AnswerVerifier 5 项校验（法规引用真实性/降级检测/冲突检测/绝对化措辞/幻觉风险）。RAG 不可用时自动降级为关键词匹配。
- **安全护栏三层防护**：Pre-Guard（PII 脱敏 + PromptInjectionDetector 6 类 32 用例注入检测）→ Post-Guard（过度承诺拦截 4 类模式 + 法规引用强制校验）→ Rate Limit（IP 滑动窗口 30req/min，/health /readyz /metrics 白名单豁免）。
- **人工复核闭环**：复核队列 API（pending/detail/reject/confirm 增强）+ 训练数据回流冲突检测（label_conflict/decision_override/model_disagreement 三类）。6 类场景自动转人工复核。
- **可观测性**：AgentStep.decision_source（RULE/MODEL/RAG_LLM/FALLBACK）+ SSE node_start/node_end 进度事件 + /readyz 深度健康检查 + /metrics Prometheus。
- **评估体系**：Golden Dataset 100 条（6 类覆盖）+ 误拒率/复核率/回复合规率 3 个新增指标。测试总数 202，离线准确率 96.19%。
- **成本控制**：分层调度——YAML 规则 + TF-IDF 处理高频场景，ChromaDB 做 RAG，模板生成回复，**MVP 阶段 0 LLM Token 消耗**。

---

### 7 波改造提交历史

| Wave | 范围 | 关键提交 |
|------|------|---------|
| 1 | 规则配置化 | YAML 规则文件 + RuleLoader + RuleEngine |
| 2 | RAG 可信度 | 评估集 50 条 + 评估器 + AnswerVerifier |
| 3 | 人工复核闭环 | pending/detail/reject API + 冲突检测 |
| 4 | 安全护栏强化 | 注入检测 32 用例 + 过度承诺拦截 |
| 5 | 可观测性增强 | decision_source Trace + SSE 进度事件 |
| 6 | 评估体系补全 | Golden Dataset 100 条 + 3 个合规指标 |
| 7 | 工程化基础 | /readyz + RateLimit + Docker healthcheck + LangGraph bugfix |
