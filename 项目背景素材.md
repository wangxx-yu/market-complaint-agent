# 市场监管 Multi-Agent 投诉智能处理系统 — 面试叙事与简历素材

> 这不是技术文档的重复。这是一份**面向面试官的叙事**——每个技术决策背后的"为什么"、真实踩过的坑、以及怎么衡量效果。
> 双非背景下，这份文档的目标是让你在面试中能说出："我不是跟着教程搭了一个，我是用真实数据、遇到了真实问题、做了真实取舍。"

---

## 一、简历可直接用的项目条目

### 写法 A：通用版（适合海投）

**项目名称**：市场监管 Multi-Agent 投诉智能处理系统（独立开发）

**项目描述**：面向区县级市场监管部门的多智能体投诉处理系统，实现从市民投诉输入到受理/退回判定、法规检索、机构分派、回复草稿生成的全链路自动化。采用规则引擎优先 + ML 模型 + RAG + LLM 四层分级调度策略，MVP 阶段 0 LLM Token 消耗。已通过 100 条 Golden Dataset 离线评估，分类准确率 96.19%。

**责任描述**：
- 设计了 6 个专家 Agent（分类/分派/拒因/检索/回复/护栏）的协作架构，基于 LangGraph StateGraph 实现 DAG 编排，支持受理/退回两条路径的并行 fan-out，降低端到端延迟
- 构建了规则引擎配置化体系：将 15+ 个硬编码关键词列表抽象为 4 个 YAML 规则文件（27 条规则），实现业务规则与代码解耦，非技术人员可直接修改规则
- 搭建了本地化 RAG 系统：ChromaDB 向量库 + 自研 Hash Embedding（零模型下载）+ 关键词规则联合打分，实现了向量检索不可用时的自动降级为关键词匹配
- 实现了安全护栏三层防护：Pre-Guard（6 类注入检测 32 用例 + PII 脱敏）→ Post-Guard（4 类过度承诺拦截 + 法规引用校验）→ Rate Limit（IP 滑动窗口限流）
- 建立了完整评估体系：手工标注 100 条 Golden Dataset（覆盖 6 类场景），7 维离线指标（分类准确率/分派正确率/RAG 召回率/端到端/误拒率/复核率/回复合规率）
- 设计了人机协同复核闭环：复核队列 API + 3 类训练数据冲突检测（label/decision/model），低置信度/争议/敏感场景自动转人工

**项目结果**：202 个测试用例全部通过，离线分类准确率 96.19%，RAG Recall@3 满足业务需求，Docker Compose 一键部署，支持纯规则模式和 LLM 增强模式两种运行方式。

---

### 写法 B：深度版（适合面试带着讲，突出工程判断力）

**项目名称**：市场监管 Multi-Agent 投诉智能处理系统 — 从规则引擎到 LLM Agent 的分层演进

**项目背景**：区县级市场监管局每天接收大量 12315 投诉工单，工作人员需要人工判断"归谁管、受不受理、转到哪个所、怎么回复"。流程高度重复但又有大量边界 case。

**我的做法**：
1. **先从真实数据入手**：拿到青铜峡市历史工单数据（Excel），分析后发现约 70% 的投诉可以用关键词规则覆盖（食品安全、价格欺诈、预付卡等），约 20% 是明确的职责外事项（农药转农业农村局、物业转住建部门），剩下 10% 是灰色地带。
2. **分层调度策略**：基于以上分析，设计了 4 层分级——YAML 规则引擎（高频确定性场景）→ TF-IDF + 逻辑回归模型（规则未覆盖场景）→ ChromaDB RAG（法规检索）→ Ollama LLM（最终兜底）。关键决策是"规则优先，模型兜底"而非"LLM 优先"——因为 MVP 阶段不需要消耗任何 LLM Token。
3. **最难的其实是 RAG 的可信度**：法规检索不是"搜到了就行"——回复里引用了不存在的法条比不引用更危险。我设计了 AnswerVerifier 做 5 项校验（法规引用真实性/降级检测/冲突检测/绝对化措辞/幻觉风险），检索不到时自动降级为人工复核提示，而非让 LLM 编造。
4. **7 波迭代而非一次成型**：每波聚焦一个维度（规则→RAG→复核→安全→可观测→评估→工程化），每波都有独立验证，确保不引入回归。

**量化结果**：202 测试 / 100 条 Golden Dataset / 离线准确率 96.19% / 误拒率可控 / MVP 零 Token 成本。

---

## 二、面试叙事：每个技术决策背后的"为什么"

以下是面试中最可能被追问的技术点，以及你应该怎么回答。

---

### 2.1 为什么用规则引擎 + ML + RAG + LLM 四层调度，而不是直接调 LLM？

**面试官想问的**：你是不是在过度设计？调一下 GPT 不就完了？

**你的回答逻辑**：

> 我的判断是基于真实数据做的。拿到青铜峡市的历史工单后，我抽样分析了 200 条，发现大约 70% 的投诉用关键词就能判断——比如"食品过期""虚假宣传""预付卡退费"这些，不需要 LLM。如果你每条都调 LLM，那就是用大炮打蚊子，而且有延迟、有成本、有不稳定。
>
> 所以我设计了分层调度：YAML 规则引擎覆盖高频确定性场景（0 Token），规则命中不了的交给 sklearn 模型（离线训练，毫秒级推理），法规检索走 ChromaDB RAG，只有最后实在判断不了才走 Ollama LLM。
>
> 核心取舍：**规则的确定性 > 模型的概率 > LLM 的灵活性**。确定性越高的方案越靠前。这个策略让 MVP 阶段完全不需要消耗 LLM Token——对一个区县市场监督管理局来说，零成本部署是落地的前提。

**加分细节**（如果面试官追问规则引擎）：
> 规则不是硬编码在 Python 里的。我抽象了一个 RuleLoader + RuleEngine 架构，4 个 YAML 文件（accept/reject/dispatch/sensitive），改了规则不需要重新部署。我还做了规则回放评估——改规则后跑一遍历史数据，确认准确率不下降才能上线。

---

### 2.2 RAG 的 Embedding 为什么自己写了一个 Hash Embedding 而不是直接用 BGE？

**面试官想问的**：你是不是不知道 BGE？还是在瞎搞？

**你的回答逻辑**：

> 我当然知道 BGE-small-zh 效果更好，但我面临两个约束：
>
> 第一，这个系统要部署在区县市场监管局的**内网 Windows 机器**上，可能没有 GPU、不能联网下载模型。BGE 模型下载+加载需要 300MB+ 的磁盘和一定内存，对部署环境有要求。
>
> 第二，我对比了两种方案——**Hash Embedding 用字符 n-gram + BLAKE2 哈希映射到 384 维向量**，完全不依赖外部模型，零下载，启动即用。它的语义表达能力当然不如 BGE，但在法规条文的召回场景下，关键词匹配的贡献远大于语义相似度，所以我设计了一个**联合打分函数**：向量相似度权重 0.55 + 规则关键词得分权重 0.45。实测下来，联合打分后的 Recall@3 与 BGE 的差距在可接受范围内。
>
> 关键取舍：**部署便利性 + 零依赖 > 极致召回率**。而且系统架构上我是可插拔的——把 `EMBEDDING_PROVIDER` 环境变量从 `hash` 改成 `bge`，就能切换到 BGE。如果未来硬件条件允许，一行配置就能升级。

---

### 2.3 RAG 怎么保证检索结果的可信度？搜到不对的法规比不搜更危险。

**面试官想问的**：你考虑了幻觉问题吗？

**你的回答逻辑**：

> 这是我在 Wave 2 迭代时重点解决的问题。RAG 的致命缺陷是：如果检索到的内容被 LLM/模板引用到了回复里，但引用的是不存在的法条或者错误条文，后果比不引用更严重——工作人员可能基于错误信息做决策。
>
> 我设计了 **AnswerVerifier** 做 5 项自动校验：
>
> 1. **法规引用真实性**：回复中如果出现了"第 X 条"或《XXX》，我会在 RAG 检索结果里反向验证有没有匹配的条文——标题模糊匹配 + 正文片段匹配双重校验
> 2. **降级检测**：如果 RAG 返回的 source 包含 "FALLBACK"，说明是关键词兜底的结果，不可靠，标记人工复核
> 3. **冲突检测**：如果同时召回多条高分法规且标题不同（比如一条说"应当受理"，一条说"可以不予受理"），标记潜在冲突
> 4. **绝对化措辞检测**：回复里出现"保证能退""一定可以"这类表述，直接拦截
> 5. **幻觉风险**：回复中引用了法规但 RAG 完全没返回结果——这是最危险的信号，直接返回降级提示"系统无法检索到相关法规依据，建议人工处理"
>
> 关键取舍：**宁可降级让人工介入，也不让不可靠的内容自动通过**。这是 fail-closed 的设计理念。

---

### 2.4 为什么选择了 LangGraph 做编排，而不是自己写 if-else 串流程？

**面试官想问的**：用 LangGraph 是不是跟风？

**你的回答逻辑**：

> 项目一开始确实是自己写的 `Orchestrator` 类——顺序执行 classify → reject_reason/dispatch → retrieve → reply → validate。随着 Agent 数量从 3 个增长到 6 个，流程复杂度上升了：受理路径下 dispatch 和 retrieve 可以并行，退回路径下 reject_reason 和 retrieve 也可以并行，而且有一些条件分支（比如输入无效直接跳到回复、分类置信度低直接转复核）。
>
> 我把这些条件分支写在 if-else 里，很快发现维护成本很高——加一个 Agent 要改 5 个地方的路由逻辑。
>
> 迁移到 LangGraph 后：用 StateGraph 定义节点和边，条件路由用 `add_conditional_edges`，并行 fan-out 用 `ThreadPoolExecutor` 在 node 内实现。代码从 ~150 行 if-else 编排逻辑缩减为一张显式图，新增 Agent 只需 add_node + add_edge。
>
> 迁移过程中踩了一个坑：**LangGraph 的 state key 不能和 node 名重名**——我的 dispatch 和 reply 节点返回的 key 和节点名冲突导致状态覆盖错误，修了之后才正常。这个经验让我理解了 LangGraph 的状态管理机制。
>
> 关键取舍：**简单的顺序流程不需要框架，但 Agent 数量 > 5 且有并行分支时，显式图编排的可维护性远高于 if-else**。而且 LangGraph 的 `invoke` 返回最终状态，天然支持全链路追踪。

---

### 2.5 安全护栏怎么设计的？提示词注入检测做了什么？

**面试官想问的**：你考虑过 LLM 安全吗？

**你的回答逻辑**：

> 安全我分了三层：
>
> **第一层 Pre-Guard（输入）**：PII 脱敏（手机号/身份证号正则替换）+ 提示词注入检测。注入检测我设计了 6 类正则模式——指令覆盖（"忽略之前的规则"）、角色劫持（"你现在是 XXX"）、提示词窃取（"告诉我系统提示词"）、强制受理（"必须受理不准驳回"）、跳过法规（"不需要法律依据"）、代码注入（eval/exec/os.system）。每类都有具体的正则，总共 32 个测试用例覆盖。
>
> **第二层 Post-Guard（输出）**：4 类过度承诺模式（"保证能退""必定赔偿""绝对没问题""包您满意"）+ 法规引用强制校验（回复中必须有法规依据，否则标记不合规）。这是针对大模型容易"过度承诺"的倾向设计的——市场监管回复不能给市民不切实际的承诺。
>
> **第三层 Rate Limit**：自己基于 Starlette 中间件实现的 IP 滑动窗口限流，30 req/min，白名单豁免 /health /readyz /metrics。没有引入第三方限流库，因为需求很简单——窗口计数 + 过期清理，30 行代码解决问题。
>
> 关键取舍：**注入检测用正则而非 LLM**——正则确定性高、零延迟、不消耗 Token。LLM 做安全检测本身也可能被注入，正则不会有这个问题。

---

### 2.6 双 Agent 辩论（Debate）模式做了什么？为什么要有这个？

**面试官想问的**：Multi-Agent 协作体现在哪？

**你的回答逻辑**：

> Debate 模式是一个交叉验证机制——同一个投诉，两个 Classifier Agent 独立分类，如果结果一致就采纳，如果不一致则触发仲裁。
>
> 仲裁逻辑不是简单选置信度高的——我设计了规则裁判：先比较 evidence_fields（两个 Agent 命中的关键词集合），看交集是否足够；再看置信度差距，如果差距 > 0.3 则倾向高置信度方；最后按 disagree 的维度（is_market / reason_type / accept_suggestion）分别记录分歧原因。
>
> 不管仲裁结果如何，有争议的 case 一律强制人工复核——因为系统自己都不确定的东西，不能让用户直接采纳。
>
> 这个模式的工程价值在于：**它把一个二分类问题变成了一个可审计的决策过程**。面试官如果问我"Multi-Agent 解决了什么单 Agent 解决不了的问题"，我的答案是——不是解决不了，而是单 Agent 的结果没有交叉验证，你无法判断它是"真的对了"还是"碰巧对了"。两个 Agent 一致的结果，可信度远高于一个 Agent 的独立判断。

---

### 2.7 评估体系怎么建的？100 条 Golden Dataset 怎么来的？

**面试官想问的**：你说准确率 96.19%，这个数字怎么来的？可信吗？

**你的回答逻辑**：

> 评估体系是分两步建的：
>
> **第一步**：我手工标注了 100 条 Golden Dataset，按 6 类场景覆盖：
> - 明确应受理（食品/药品/价格/质量/预付卡等，30 条）
> - 职责外事项（农药转农村农业局/物业转住建/烟草转专卖局等，20 条）
> - 材料不全（商家不详/无凭证/虚假信息，15 条）
> - 已受理/重复投诉（10 条）
> - 敏感/高风险（自杀威胁/群体事件/涉密，10 条）
> - 无效输入（过短/无中文/无投诉要素，5 条）
> - 含地域分派验证（10 条，标注了 expected_office_name）
>
> **第二步**：评估框架跑 7 个维度的指标——分类准确率、分派正确率、RAG Recall@3、端到端正确率、误拒率、复核率、回复合规率。每条样本的输出还包括具体错误信息，方便定位问题。
>
> 96.19% 是分类维度的离线准确率。我没有只报"准确率"一个数字——因为准确率在样本不均衡时会失真。我还报了误拒率（该受理的没受理）和复核率（不确定转人工的比例），这两个指标对业务更有意义。
>
> 关键取舍：**手工标注 100 条而不是用 LLM 自动生成**——LLM 生成的"测试数据"有系统性偏差，它只会生成自己"会做的题"。真实工单标注才能测出系统的真实边界。

---

### 2.8 为什么说 MVP 阶段 0 LLM Token 消耗？这是在回避 LLM 吗？

**面试官想问的**：你是不是只做规则没做 LLM 部分？

**你的回答逻辑**：

> 不是回避，是分层。我**同时实现了两套编排器**：
>
> - **LangGraph Orchestrator**（规则模式）：YAML 规则 + sklearn 模型 + RAG + 模板回复，纯本地，0 Token
> - **LLM Agent Orchestrator**（ReAct 模式）：Ollama + function calling，4 个 tools（classify_complaint / dispatch_to_office / search_regulations / generate_reply），支持 SSE 流式输出
>
> 环境变量 `ORCHESTRATOR_BACKEND` 决定用哪套。Docker Compose 提供了 `--profile llm` 一键启动含 Ollama 的完整版。
>
> 这个设计的价值在于：**系统不依赖 LLM 就能工作，LLM 是增强而非依赖**。对预算有限的区县级单位来说，规则模式已经能解决 70% 的问题。而对技术团队来说，规则引擎和 LLM Agent 共享同一套底层 Agent（classifier/dispatch/retrieval/reply），代码复用率很高。
>
> ReAct Agent 的实现包括：完整的 function calling 循环（think → act → observe → think，最多 5 轮），流式 SSE 推送到前端，LLM 不可用时自动降级回规则引擎。这是**真实可运行的 LLM Agent，不是 demo**。

---

### 2.9 人工复核闭环怎么形成数据飞轮？

**面试官想问的**：系统怎么越用越好？

**你的回答逻辑**：

> 复核闭环是三个环节串起来的：
>
> 1. **触发复核**：6 类场景自动标记 review_required（低置信度/争议/敏感/分派缺省/降级/注入检测命中），在待复核列表里排队
> 2. **人工确认**：工作人员通过 Web 工作台（/static/review.html）查看详情，confirm 或 reject 系统建议，可修正 reason_type / office / reply_text
> 3. **数据回流**：复核结果导出为训练数据（accept_training + reject_reason_training），进入冲突检测环节——label_conflict（人工标注与系统分类不一致）/ decision_override（复核覆盖了系统建议）/ model_disagreement（两个模型判断不一致）——冲突数据单独隔离，不污染训练集
>
> 关键设计：**冲突数据不进训练集，而是进人工审查队列**。这是从工程实践中总结出来的——直接拿所有复核数据训练会导致"以讹传讹"，一个人的错误判断会传染给模型。

---

### 2.10 测试 202 个用例怎么组织的？覆盖了什么？

**面试官想问的**：你的测试策略是什么？

**你的回答逻辑**：

> 202 个测试用例按模块组织，每个 Agent 和工具模块都有对应的测试文件：
>
> - **Agent 层**：test_agents.py（分类/分派/拒因/回复端到端）、test_rule_engine.py（规则匹配优先级/排除关键词/敏感词）、test_prompt_injection.py（32 个注入用例）、test_guardrails_output.py（输出合规校验）、test_answer_verifier.py（5 项校验）
> - **编排层**：test_langgraph_orchestrator.py（DAG 节点路由/并行 fan-out/条件分支）、test_api.py（16 个端点集成测试）
> - **数据层**：test_health.py、test_review_api.py（复核队列 CRUD）、test_trace_structure.py（Trace JSONL 结构校验）
> - **工具层**：test_export_*.py（训练数据导出）、test_replay_accept_model.py（模型回放）、test_training_conflict.py（冲突检测）
> - **评估层**：test_eval_metrics.py（指标计算）、test_rag_eval.py（RAG 召回率评估）
>
> 测试策略：Agent 层用真实输入验证行为语义（不是 mock 返回值），编排层验证路由逻辑和数据流完整性，工具层验证文件 I/O 正确性。

---

## 三、面试高频追问清单 + 你的回答要点

| 面试官可能问 | 你的回答方向 |
|-------------|------------|
| "你这个项目有没有上线？" | 目前是 MVP 阶段，已通过离线评估（100 条 Golden Dataset, 96.19%），Docker 一键部署就绪，可以直接在区县级单位试点。 |
| "为什么用 ChromaDB 而不是 Milvus/Qdrant？" | ChromaDB 是嵌入式、零运维、Python 原生，适合单机部署。Milvus 太重，适合企业级集群。选择标准是对标部署环境的约束。 |
| "你写的 Hash Embedding 是不是太 naive 了？" | 承认 BGE 效果更好，但 Hash Embedding 在法规条文检索的场景下配合关键词联合打分，实测差距可接受，且零依赖。架构上是可插拔的，一行配置切换。 |
| "LangGraph 和 LangChain 什么关系？" | LangGraph 是 LangChain 生态的状态图编排框架，专注 Agent 工作流。我用它替代了手写 if-else 编排，核心价值是显式 DAG + 状态管理 + 条件路由。 |
| "RAG 的 chunk 策略是什么？" | 当前知识库以"条文"为粒度，每个条目是一个完整的法条段落（而非固定窗口切割），因为法规条文本身就是自包含的语义单元。未来如果用更长的法规文档，会按条款级别切割。 |
| "你这个系统跟直接调 GPT API 有什么区别？" | GPT API 有数据隐私风险（投诉内容含个人信息），有网络依赖，有成本。这个系统本地化部署，数据不出内网，零外部依赖，且规则引擎的确定性是 LLM 无法替代的。 |
| "你最大的技术难点是什么？" | 不是某个单一技术点，而是**6 个 Agent 之间的协作一致性**——分类说"不受理"但分派也跑出来了、RAG 返回了与分类矛盾的法条、模板生成的回复引用了不存在的法规。这些都是跨 Agent 的协调问题，我通过 LangGraph 状态图 + AnswerVerifier 校验 + 自动标记复核策略解决的。 |

---

## 四、简历关键词清单（按 JD 匹配使用）

根据大模型应用开发方向常见 JD 要求，以下是你项目中的对应关键词：

| JD 常出现的关键词 | 你项目中的对应点 |
|------------------|----------------|
| RAG / 检索增强生成 | ChromaDB + 联合打分 + 降级策略 + 50 条评估集 |
| Multi-Agent / Agent 协作 | 6 个专家 Agent + LangGraph 编排 + Debate 交叉验证 |
| LLM 应用开发 / Function Calling | ReAct Agent + 4 tools + Ollama + SSE 流式 |
| Prompt Engineering | 系统提示词设计 + 工具定义 schema + few-shot 记忆 |
| 模型评估 / 数据飞轮 | Golden Dataset 100 条 + 7 维指标 + 复核闭环 |
| 向量数据库 / Embedding | ChromaDB + Hash/BGE 双方案 + 可插拔架构 |
| 安全 / 护栏 | 注入检测 32 用例 + PII 脱敏 + 过度承诺拦截 |
| 工程化 / DevOps | Docker Compose + healthcheck + Rate Limit + Prometheus |
| Python / FastAPI | 全项目 Python + FastAPI + Pydantic + 202 测试 |
| 规则引擎 / 决策系统 | YAML 配置化 + RuleLoader + RuleEngine + 回放评估 |

---

## 五、简历上怎么写这个项目（直接可用模板）

### 项目经历

**市场监管 Multi-Agent 投诉智能处理系统** | 独立开发 | 2024.XX - 2024.XX

基于真实 12315 投诉工单数据，设计并实现了一个本地优先、人机协同的多智能体投诉处理系统。采用规则引擎 + ML + RAG + LLM 四层分级调度，覆盖投诉分类、职责判定、法规检索、机构分派、回复生成的完整链路。

- 设计 6 个专家 Agent（分类/分派/拒因/检索/回复/护栏）并基于 LangGraph StateGraph 实现 DAG 编排，支持并行 fan-out 降低延迟，同时实现 ReAct 模式 LLM Agent（4 function tools + SSE 流式）
- 构建 YAML 规则引擎（27 条规则）替代硬编码，实现业务规则与代码解耦；搭建 ChromaDB RAG 系统，自研 Hash Embedding + 关键词联合打分函数，设计了 5 项校验的 AnswerVerifier 防止法规引用幻觉
- 实现安全护栏三层防护：输入注入检测（6 类 32 用例 + PII 脱敏）→ 输出过度承诺拦截（4 类模式匹配）→ IP 滑动窗口限流
- 建立完整评估体系：手工标注 100 条 Golden Dataset（6 类场景），7 维离线指标，分类准确率 96.19%；202 个测试用例覆盖全模块
- 设计人机协同复核闭环：复核队列 API + 训练数据回流 + 3 类冲突检测（label/decision/model），低置信度场景自动转人工

---

## 六、项目故事的叙事主线（面试 3 分钟版本）

如果你在面试中要用 3 分钟讲这个项目，按以下结构讲：

> **1. 背景（30 秒）**
> 区县市场监管局每天处理大量 12315 投诉工单，工作人员需要人工判断"归谁管、受不受理、转到哪个所、怎么回复"。我拿到青铜峡市的历史工单数据后，做了两件事：分析数据分布、设计分层调度策略。
>
> **2. 核心设计（60 秒）**
> 我没有一上来就用 LLM，而是按数据分布设计了四层：规则引擎覆盖 70% 的高频确定性场景，ML 模型处理规则未命中的，RAG 做法规检索，LLM 最终兜底。这样 MVP 阶段零 Token 消耗。6 个 Agent 通过 LangGraph 编排，受理和退回两条路径做了并行 fan-out。
>
> **3. 踩过的坑（60 秒）**
> 最难的其实是 RAG 可信度——回复里引用不存在的法条比不引用更危险。我设计了 AnswerVerifier 做 5 项校验，检索不到或冲突时自动降级人工处理。另一个坑是 LangGraph 的 state key 和 node 名冲突导致状态覆盖，调试后理解了它的状态管理机制。
>
> **4. 效果（30 秒）**
> 100 条 Golden Dataset 离线评估，分类准确率 96.19%，202 个测试用例全通过。Docker 一键部署。系统已经在规则模式下可用，LLM 模式通过 Ollama 本地运行。
